Schibsted Grotesk (ExtraBold) and Fragment Mono (Regular), SIL Open Font License 1.1,
from Google Fonts. Vendored only so the OG image (`lib/og/card.tsx`) renders at build
without a network fetch. Pages load the same families through `next/font/google`.
